# HAJ API reference

This wiki is generated automatically from TSDoc comments in the codebase.

It covers utilities, models, services, hooks, stores, and related modules. Regenerated on each push to `main` when documented source files change.

To build locally:

```bash
pnpm run docs:wiki
```

Output is written to `build/wiki-docs/`.
